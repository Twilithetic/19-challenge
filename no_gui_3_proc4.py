
import cv2



def print_image(frame):
        # ?????????????
    # 1. ????????????????????1600x900?
    max_display_width = 1024  # ??????
    max_display_height = 600  # ??????
    
    # 2. ????????
    h, w = frame.shape[:2]
    
    # 3. ??????????????????
    scale = min(max_display_width / w, max_display_height / h)  # ????????????
    new_w = int(w * scale)  # ??????
    new_h = int(h * scale)  # ??????
    
    # 4. ????
    frame_scaled = cv2.resize(frame, (new_w, new_h))
    
    # 5. ????????
    cv2.imshow("A4 Detection", frame_scaled)
    cv2.waitKey(0)
    cv2.destroyAllWindows()